/**
 * Kept as the stable import path for icon detection; the implementation lives
 * in ./icon-finder/.
 */
export {
  findProjectIcon,
  findProjectIconCandidates,
  type IconCandidate,
} from './icon-finder/index';
