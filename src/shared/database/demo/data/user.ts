/**
 * Identity shown in demo mode.
 *
 * The /users/current route reads the OS username directly rather than the
 * database, so demo mode has to substitute this value or the real username
 * appears in published screenshots.
 */
export const DEMO_USER_NAME = 'dev';
